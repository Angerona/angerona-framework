package com.whiplash.res;

/**
 * This enum stores keys for icon resources.
 * @author Matthias Thimm
 */
public enum WlIcon {
	/** Icon for a "close" action (active or hovered). */
	CLOSE_ACTIVE,
	/** Icon for a "close" action (inactive). */
	CLOSE_INACTIVE,
	/** Icon for a "show more" action (active or hovered). */
	MORE_ACTIVE,
	/** Icon for a "show more" action (inactive). */
	MORE_INACTIVE,
	/** Icon for a "next element" action (active or hovered). */
	NEXT_ACTIVE,
	/** Icon for a "next element" action (inactive). */
	NEXT_INACTIVE,
	/** Icon for a "previous element" action (active or hovered). */
	PREVIOUS_ACTIVE,
	/** Icon for a "previous element" action (inactive). */
	PREVIOUS_INACTIVE,
	
	/** Icon for a "new file" action (active or hovered). */
	NEWFILE_ACTIVE,
	/** Icon for a "new file" action (inactive). */
	NEWFILE_INACTIVE,
	/** Icon for a "new file" action with more alternatives (active or hovered). */
	NEWFILE_MORE_ACTIVE,
	/** Icon for a "new file" action with more alternatives (inactive). */
	NEWFILE_MORE_INACTIVE,
	/** Icon for a "save" action (active or hovered). */
	SAVE_ACTIVE,
	/** Icon for a "save" action (inactive). */
	SAVE_INACTIVE,
	/** Icon for a "save as" action (active or hovered). */
	SAVEAS_ACTIVE,
	/** Icon for a "save as" action (inactive). */
	SAVEAS_INACTIVE,
	/** Icon for an "open" action (active or hovered). */
	OPEN_ACTIVE,
	/** Icon for a "open" action (inactive). */
	OPEN_INACTIVE,
	
	/** Image indicating an invalid selection. */
	INVALID,
	
	/** General preferences tool bar icon. */
	PREFERENCES_GENERAL,
	/** Typesetting preferences tool bar icon. */
	PREFERENCES_TYPESETTING,
	/** Fonts & colors al preferences tool bar icon. */
	PREFERENCES_FONTS_AND_COLORS
}
