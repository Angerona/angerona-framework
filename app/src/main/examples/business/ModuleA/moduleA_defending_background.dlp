% knowledge about claires reasoning
reason(se) :- answer(se, p_lie, _).
reason(wd) :- answer(wd, P_lie, _).
reason(acquire) :- not reason(wd), not reason(se).

% penalty points for bad behavior
penalty(66) :- reason(wd).
penalty(33) :- answer(wd, p_honest, _).

% constraints to give up answer set
:- reason(acquire).
:- penalty(X), X>=66.